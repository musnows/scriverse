{
  "name": "@musnows/scriverse",
  "version": "0.9.4",
  "description": "Command-line client and local server for the Scriverse long-form fiction workspace",
  "license": "AGPL-3.0-only",
  "author": "musnows",
  "homepage": "https://scriverse.top/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/musnows/Scriverse.git"
  },
  "bugs": {
    "url": "https://github.com/musnows/Scriverse/issues"
  },
  "keywords": [
    "scriverse",
    "novel",
    "writing",
    "cli"
  ],
  "type": "module",
  "files": [
    "dist/**/*"
  ],
  "bin": {
    "scriverse": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "cli": "tsx src/cli.ts",
    "clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && tsc -p tsconfig.build.json && node scripts/copy-public.mjs",
    "prepack": "npm run build",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:database-upgrade": "vitest run tests/integration/database-upgrade-fixture.test.ts --maxWorkers=1",
    "test:integration": "vitest run tests/integration",
    "test:system": "vitest run tests/system",
    "test:e2e:real": "tsx tests/e2e/real-all-features.ts",
    "test:e2e:reader-fixture": "tsx tests/e2e/browser-reader-fixture.ts",
    "test:e2e:cli": "npm run build && tsx tests/e2e/real-cli.ts",
    "test:e2e:ai-tools": "tsx tests/e2e/real-ai-agent-tools.ts",
    "test:e2e:browser-fixture": "tsx tests/e2e/browser-ai-fixture.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1102.0",
    "@smithy/node-http-handler": "^4.9.13",
    "docx": "^9.7.1",
    "express": "^5.1.0",
    "jszip": "^3.10.1",
    "mammoth": "^1.10.0",
    "multer": "^2.0.2",
    "pinyin-pro": "^3.28.2",
    "sharp": "^0.35.3",
    "undici": "^7.29.0",
    "vditor": "^3.11.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.10.0",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
